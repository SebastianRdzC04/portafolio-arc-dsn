services:
  portafolio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "2026:80"
    environment:
      - PDF_RENDER_ORIGIN=http://127.0.0.1:80
    restart: unless-stopped
